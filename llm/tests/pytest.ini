[pytest]
markers =
    integration: tests that require real external dependencies (grpc, ollama, etc.)
    server: mark test as a server-level test
    model: mark test as a direct model test
    grpc: mark test as a gRPC integration test